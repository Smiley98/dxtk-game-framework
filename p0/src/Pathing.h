#pragma once
#include "TileMap.h"
#include "MathUtilities.h"
#include <vector>

namespace Pathing {
    //using Path = std::vector<Cell>;

    // Returns the resultant position based on speeed along a path from start to end
    //Vector3 FollowPath(const Vector3& start, const Vector3& end, float speed, const TileMap& map);
    //
    //// Returns a vector of tile grid indices to traverse to get from start to end (using A*).
    //Path FindPath(const Cell& start, const Cell& end, const TileMap& map);
    //
    //// Returns adjacent cells which can be moved to (tiles who's value is EMapValue::AIR).
    //std::vector<Cell> GetNeighbours(const Cell& cell, const TileMap& map);
    //
    //// Render path tiles
    //void DrawPath(const Path& path, const TileMap& map);
};